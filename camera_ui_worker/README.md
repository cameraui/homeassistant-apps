# camera.ui Worker

Turns this machine into a worker for a camera.ui master running elsewhere. It takes over frame decoding and plugins the master assigns, so heavy work (hardware decoding, object detection) runs here instead of on the master.

Full documentation is on the Documentation tab.
